<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @book.name %>
</p>

<p>
  <strong>Author:</strong>
  <%= @book.author %>
</p>

<p>
  <strong>User:</strong>
  <%= @book.creator.username %>
</p>

<p>
  <b>Cover:</b>
  <%= image_tag @book.book_image.url %>
</p>

<p>
  <strong>Recommendations:</strong>
  <ul>
    <% @book.recommendations.each do |recommendation| %>
      <li><%= recommendation.type.name %></li>
      <li><%= link_to recommendation.name, recommendation_path(recommendation) %></li>
      <li><%= recommendation.description %></li>
      <li>
        Submitted <%= time_ago_in_words(recommendation.created_at) %> ago
        by <%= recommendation.user.email %>
      </li>
      <%= link_to like_recommendation_path(recommendation), method: :put do %>
        Upvote
        <%= recommendation.get_upvotes.size %>
      <% end %>

      <%= link_to dislike_recommendation_path(recommendation), method: :put do %>
        Downvote
        <%= recommendation.get_downvotes.size %>
      <% end %>
    <% end %>
  </ul>
</p>

<%= link_to 'New Recommendation', new_recommendation_path %>